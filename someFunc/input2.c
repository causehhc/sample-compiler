int var=expr;
int var (int);
int_t main (){
    int var = expr, var = expr;
    var = var(expr);
    if(expr){
        int var = expr;
        while(expr){
            var = expr;
            var = var(expr);
        }
    }
    return expr;
}
int var (int var){
    int var = expr;
    return expr;
}
